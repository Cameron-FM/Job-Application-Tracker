{
  "name": "job-tracker",
  "version": "1.9.0",
  "private": true,
  "description": "Personal job application tracker (mini-ATS) — runs locally, data stays on disk",
  "scripts": {
    "postinstall": "npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"node server/index.js\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix client run build && node server/index.js",
    "seed": "node server/seed.js",
    "import": "node scripts/import-job.js",
    "import-contact": "node scripts/import-contact.js",
    "scan-documents": "node scripts/scan-documents.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "express": "^4.21.2",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "allowScripts": {
    "better-sqlite3@12.11.1": true
  }
}
